# MVP_SPEC：课表入历
2026-09-11 / v0.2 本地迭代。当前指令优先于旧 V1 “不做 OCR”的限制。

## 产品与用户
面向中国时区上课、习惯 Apple Calendar 的大学生。
课程基本信息只登记一次，再安排每周多个时段；图片识别用于预填草稿，最终人工核对。
用户亲测 WakeUp 方便，本工具优势仍需用相同课表比较耗时、错误率和偏好验证。

## 当前流程
1. 手动登记课程名称、教师、默认教室，进入“已登记课程”列表。
2. 点击课程，多选星期，每天编辑自己的起止时间、周数、单双周和教室。
3. 同一天可增加多个时段；课程名称、教师、提醒全课程共享。时段教室为空时使用默认教室。
4. 点击“保存时间并确认这门课”。修改任意信息后恢复待核对，改变学期起始日后全部待核对。
5. 全部课程确认后，查看真实日期并下载 ICS。

## 识图 v0.2
- 选择 PNG/JPG/WebP，最大 16 MB、2400 万像素，预览原图后点击识别。
- 使用 Tesseract.js 6.0.1 + 中英文模型在浏览器内识别，图片不上传，无 API 密钥或业务后端。
- API 层脚本已随 dist/vendor 分发；Worker、WASM 与语言模型首次通过 CDN 下载，因此“本地计算”不等于首次断网可用。资源下载失败时展示错误，可重试或粘贴相册识别文字。
- 根据 OCR 字框尝试定位星期列、合并单元内容；识别明示的教师/地点、钟点时间、周数及单双周。当前为规则预填，不是视觉大模型。
- 生成候选课程，支持修改候选名称/取消勾选；登记时仅完全同名且教师相同的课程合并，不覆盖已有时间；完全相同的时段去重。
- 登记后全部标为待核对。无法读出钟点时间时留空，不能把学校“第3-4节”擅自变成固定钟点。
- 未读出周数时草稿暂用 1-16 周，必须对照原图修改确认。未定位星期时保留课程、不猜上课日。
- 保留识别原文供对照，可改文字后重解析；复杂表格、彩色底、换行课名、无标签教师、合并单元格仍可能漏识别/串行。
- 提供进度、取消、超时和无结果处理；识别过程及失败不清空已登记课程。

## 技术及数据模型
纯静态 HTML/CSS/JavaScript，无构建。dist 可独立静态托管，或双击 index.html 本地使用；浏览器对识图网络资源的兼容仍需实机测试。
Course：id/name/teacher/location/reminder/sessions/needsReview/sourceText。
Session：id/day/start/end/weeks/parity/location/sourceText。
course-model.js 负责规则验证、草稿合并与展开；course-import.js 负责 OCR 数据/文字解析；ocr.js 负责识别生命周期；calendar.js 保留已验证的 ICS 生成。
所有输入仅在本页内存中，刷新丢失；离开提示不等于备份。

## 时间与导出边界
中国标准时间 UTC+8；第一教学周周一；1–30 周、范围/自定义周数/单双周。
每次课程独立 VEVENT；UID 包含课程 ID 和时段 ID，支持同日多时段。
UTC 日期换算、CRLF、75 字节中文安全折行及文本转义，预览和导出使用同一事件计算。
不自动调休、节假日或跨午夜；学校节次映射、草稿持久化、云同步、账号、支付、自动写日历仍未实现。
课程未确认/时间不完整时阻止整份导出，不静默漏掉待核对课程。时间重叠提示供判断，不阻止已确认导出。

## Apple Calendar
Mac 日历中新建专用 iCloud 日历 → 文件/导入 → 选择 .ics → 核对首末周、单双周和提醒。
跨设备由用户同账号下的 iCloud 日历负责。iPhone/iPad 直接导入入口和 Apple 真机行为仍待验证。
ICS 为一次性导入，网页更改不会同步更新；重复导入可能重复事件，更新前处理旧课表日历。
参考：[Apple 导入说明](https://support.apple.com/en-gb/guide/calendar/icl1023/mac)、[RFC5545](https://www.rfc-editor.org/rfc/rfc5545)。

## 内测及定价
前 33 位真实内测用户免费，要求完成导入并反馈；稳定后 9.99 元试水，当前无收费/发布。
7 天小实验、3–5 条真实演示；记录完成率、日期准确率、录入/核对耗时、人工服务时间与实际成交。
33 人中争取 20 人完成、5 人提出具体付费理由，仅为探索阈值，非收入预测。
新增 OCR 指标：课程召回/重复数、教师与时间错配数、每门纠错时间、首加载成功率。
若核对识图结果比手输更慢，先改解析与输入体验，不以“识图”包装成已解决需求。学习优先，考试周暂停招募。

## 验证现状
node test.cjs：原 ICS 周数、时间、跨年、中文等回归。
node test-v02.cjs：模型及离线模拟 DOM 交互，多日/同日多时段、独立单双周、唯一 UID、确认拦截、同名合并、真实下载内容、OCR 取消。
work/v02-tests/ocr-smoke.cjs：合成课表经实际 Tesseract OCR，核对两门中文课名、星期和钟点。
合成图片不是实际学校课表准确率证明；尚未完成浏览器端整链路、Apple 真机或手机内存/性能验收。
来源：[Tesseract.js API](https://github.com/naptha/tesseract.js/blob/v6.0.1/docs/api.md)、[本地资源说明](https://github.com/naptha/tesseract.js/blob/v6.0.1/docs/local-installation.md)。

## v0.2.1 本地启动修正（覆盖前文首次联网/双击识图说明）
用户实测 file:// 模式触发 opaque origin 错误，不能以原直接双击方式承诺识图兼容。
新增 serve.cjs 和“启动课表工具.cmd”，仅绑定 127.0.0.1:4173、只读提供 dist 内静态文件，无图片上传接口。
识别 Worker、四种 core wasm.js 和中英文 traineddata 均随 dist/vendor/ocr 提供。显式同源 Worker、workerBlobURL:false、gzip:false，不使用外部 CDN。
file:// 识图立即提示正确启动方式；引擎加载超时与图片识别超时区分，错误不再统一归因于图片清晰度。
本地启动仍是静态文件服务，不是需要部署购买的业务服务器。用户输入内存状态不会跨地址自动迁移。
