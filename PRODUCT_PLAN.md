# 课表 → Apple Calendar 产品规划基线

> **当前产品规划版本：2026-09-12 / v1.0。** 本文是本次用户明确要求的后续规划基线，独立于已有文档新增，不代表功能已经实现或验收。后续开发先读本文，再核对 MVP_SPEC.md、APPLE_CALENDAR_PLAN.md、PROJECT_CONTEXT.md 和实际代码。
> 本文更新产品分层、试验价格和未来订阅方向；保留旧文档中的实现记录与有效约束。V1/V2 是产品阶段，不等同于已有代码的 v0.2/v0.3。已有 OCR 不删除、不重写，作为可选辅助输入，不能阻塞手动闭环。未决问题与验证结果须继续补充到本文。

## 1. 产品定位、用户与边界

面向大学生的轻量课表 → Apple Calendar 工具：在网页整理课表规则，交给系统日历展示和提醒。目标用户是已经使用或愿意使用 Apple Calendar、希望课程与个人安排放在一起、又不想维护另一个重课表 App 的大学生。

核心痛点：
- 逐次建立整学期事件繁琐，单双周、不连续周容易出错。
- 学校课表的教学周、节次与实际日期/钟点之间需要转换。
- 时间、地点、教师分散，导入后难以核对，调课后重新导入麻烦。
- 需要清晰的成功引导与失败补救，而非只拿到一个文件。

项目负责人是**大二学生，学习优先**。产品必须低成本、低维护；考试周可以停止招募，但已经售出的 Pro 学期服务仍须履约。建议先以每周 5–7 小时为投入上限试行，这只是可调整预算，不是用户已作出的工时承诺。

不扩展为社交、校园社区、成绩管理、教务账号抓取或重课表 App；当前不做双向日历同步、原生 App、学校适配大全和持续付费 AI 服务。

## 2. WakeUp 带来的启示与差异化假设

用户亲测 WakeUp 导入方便，这是需求判断的重要反证：不能把“能生成日历”当作独家优势，也不能先假定手动录入更省时。本文不对 WakeUp 当前价格、功能、体积或销量作未经核验的断言。

本产品只做**极简导入/订阅层**。候选差异化：打开网页即可用、Basic 不要账号且本地处理、专注 Apple Calendar、日期预览可核对、Pro 修改后无需再导入文件。用同一份真实课表对比录入与核对耗时、错误数量、完成率和用户偏好；若没有优势，缩减或停止投入。

## 3. 产品分层与价格试验

| 项目 | V1 Basic | V2 Pro |
|---|---|---|
| 核心价值 | 一次性导入整学期 | 本学期订阅一次，后续修改由日历拉取 |
| 试验价格 | 6.9 元 | 9.9 元 |
| 输入与预览 | 手动录入、规则校验、整学期预览 | 复用 Basic 编辑与预览 |
| 输出 | 浏览器生成并下载 .ics | 专属 HTTPS .ics 订阅 URL，建议同时保留下载 |
| 修改 | 网页修改后重新导出；处理旧事件 | 网站保存后，Apple Calendar 后续刷新获取 |
| 数据与服务 | 纯前端，无账号/数据库/业务 API | 最少必要的线上保存、编辑鉴权和订阅读取 |
| 有效期 | 文件交付；无持续同步承诺 | 至少覆盖购买时明确的当前学期 |

**前 33 名内测免费。** 记录真实参与人数，不虚构稀缺。Basic 6.9 元、Pro 9.9 元仅是验证阶段价格，根据实际转化、用户反馈与服务成本调整，替代历史 9.99 元单档试水建议。可探索 Basic 补 3 元升 Pro，但升级资格、学期和履约流程确认后才对外提供。

纯前端 Basic 无法可靠保护付费权限，也不自带自动支付验单能力。首轮免费验证，收费阶段可先采用低人工成本的外部交付流程；如需要自动验单，应单独评估，不能一边声称零业务后端，一边隐含自建支付服务。本文不授权现在收款或上线。

## 4. V1 Basic 范围

必做：
1. 学期设置：名称、第一教学周周一、总周数、时区。
2. 手动填写课程名、教师、地点、星期、节次或起止时间、周次、单双周、提醒。
3. 一门课程支持多个独立时段；时段可覆盖地点、周次、单双周。
4. 节次模式必须由用户设置本校节次 → 钟点映射，不能猜测统一作息。
5. 展开整学期真实日期，按周/列表预览，显示事件数和冲突；确认后生成 .ics。
6. 纯前端生成 UTF-8 文件，无需账号、数据库、业务 API 或传统服务器；网站静态托管与本地静态预览不等于业务后端。
7. 明确 Apple Calendar 添加步骤与结果核对，不能静默写入日历。

建议随基础可用性一起完成本地草稿保存、清除及 JSON 备份/恢复；私密模式、存储失败时明确提示。当前资料记载输入仅在内存，后续须以代码核验实际状态。已有浏览器 OCR 保留为可选预填，输出必须人工确认，不承诺全校通用或免核对。

## 5. V2 Pro 范围与技术选择

网站是编辑入口，Apple Calendar 是展示入口。每个用户拥有可管理的课表及独立只读订阅 URL；用户添加订阅后，修改课程、教室、时间或删课，客户端后续拉取最新日历。**这是单向订阅，非即时推送或双向同步。** 不承诺几秒内刷新，不假定所有设备刷新行为相同。

Basic 保持现有轻量前端；Pro 从 Vercel / Cloudflare / Supabase / Firebase 等 Serverless/BaaS 中选择一套最简可行组合，不购买传统服务器，不把四家全部叠加。后续项目按目标网络可达性、免费额度限制、休眠策略、数据存储、备份、域名和维护负担核验后作出选择。免费额度不是永久零成本保证。

先完成订阅技术验证，再建设最小编辑鉴权；不先做复杂会员系统。可选托管登录或独立编辑凭证，但必须支持恢复/撤销，不能拿公开订阅链接充当编辑权限。

## 6. 数据结构建议

兼容现有 Course + Session 模型，增量迁移并标注 schemaVersion。

| 实体 | 建议字段 |
|---|---|
| Timetable | id、schemaVersion、semesterName、week1Monday、weekCount、timezone、periodMap、courses、revision、updatedAt |
| Course | id、name、teacher、defaultLocation、reminderMinutes/null、sessions、needsReview |
| Session | id、weekday(1–7)、inputMode、periodStart/periodEnd 或 startTime/endTime、weeks、parity(all/odd/even)、locationOverride |
| Exception（后续） | occurrenceId、type(cancel/move)、newDate/newStart/newEnd、新地点 |
| Subscription（Pro） | id、ownerId 或编辑凭证摘要、timetableId、只读令牌摘要、expiresAt、status、revision、createdAt/updatedAt |

课程、时段和具体课次用持久稳定 ID；Basic 保存草稿时保存 ID，Pro 迁移时保留。不收学号、教务密码或 Apple 账号密码。提醒允许关闭；多个时段默认继承课程提醒。

## 7. 日期与 ICS 生成规则

- 先将输入周次（如 1-8,10,12-16）规范化为去重、升序的整数集合，再与单双周条件求交。奇偶判断使用**教学周编号**，不是自然年周数。
- 日期 = 第一周周一 + (教学周 - 1)×7 天 + (星期 - 1) 天。学期跨月、跨年仍使用日期运算；限制周次在本学期范围，空集合应提示。
- 节次先按已确认的学校作息转换；日期/时间缺失、结束不晚于开始、未确认课程阻止导出，不静默漏课。跨午夜暂不支持，明确提示。
- 建议继续每次上课生成独立 VEVENT，直接覆盖单双周与不连续周；V1 不必引入复杂 RRULE。预览和 ICS 使用同一展开结果。
- 使用 VCALENDAR、VERSION:2.0、PRODID；每事件提供 UID、DTSTAMP、DTSTART、DTEND、SUMMARY，地点映射 LOCATION，教师等放 DESCRIPTION。结束时刻按非包含边界处理。
- 默认 Asia/Shanghai。建议继续将当地时间正确转换为 UTC 后以 Z 输出，避免浮动时间；如改用 TZID，提供一致的 VTIMEZONE。未来支持其他时区须测试夏令时，不能一律加减 8 小时。
- 文本转义反斜杠、逗号、分号与换行；用 CRLF 分行，UTF-8 安全折行（每内容行不超过 75 octets，延续行加空格），不能拆坏中文字节。
- 提醒使用 VALARM（DISPLAY、DESCRIPTION、负 TRIGGER），实际响铃受客户端导入/订阅和通知设置影响，须实机确认。
- Pro UID 基于不可变的课表/课程/时段/原课次身份，修改时间地点不应重建 UID；发生变化时更新 SEQUENCE、LAST-MODIFIED。DTSTAMP 不因每次读取任意变化。取消或移动课次须验证客户端删除/取消行为，不能只测试新增。
- **稳定 UID 不等于 Basic 重复导入必然去重或覆盖。** 旧 APPLE_CALENDAR_PLAN.md 中以课名/时间哈希身份、重复导入必然更新的方案不作为已验证保证。Basic 建议导入独立课表日历，重导前指导用户备份并处理旧日历；不自动删除个人日历事件。

格式依据：[RFC 5545](https://www.rfc-editor.org/rfc/rfc5545)。以上为实现约束，不表示当前代码全部满足。

## 8. 订阅 URL 架构与生命周期

建议接口职责（命名可调整）：
- 受鉴权的创建/读取/保存课表接口；校验输入和所有权，以 revision 防止多页面覆盖。
- GET /cal/{高熵随机令牌}.ics：无需交互登录即可只读访问，返回 text/calendar; charset=utf-8；只读令牌不可推导、不可枚举，与编辑凭证分开。
- 支持 HTTPS、ETag/Last-Modified、条件请求；修改后正确失效缓存。URL 保持稳定，读取不修改数据，失败不返回伪成功空日历。
- 可提供 webcal 打开入口，同时提供 HTTPS 地址及复制说明；它们是同一订阅源，不把下载地址误称为同步。
- 每次保存原子提交，生成失败保留上一次有效版本；提供用户可见的保存状态、最后修改时间。订阅拉取失败可重试，短暂服务故障不能清空用户已有课表。
- 每个购买的订阅至少覆盖页面明确显示的当前学期结束日期；不承诺永久。到期前提示续期/导出，写明到期后的保留或停用规则，再对外销售。
- 凭证可撤销/轮换；轮换订阅链接需告知重新订阅。旧链接不分配给新用户。停用或删号后远端返回状态须明确，但不能保证客户端立即清除缓存。
- 上线前落实备份/恢复、成本告警、限流和最小可用监测。日志隐藏完整令牌和课程内容；不能让带令牌 URL 进入第三方分析或公开索引。

拥有订阅链接的人可能读取课表，必须在生成页说明。官网订阅操作参考：[Apple：使用 iCloud 日历订阅](https://support.apple.com/en-mide/102301)。刷新延迟和跨设备效果需按实际设备验证。

## 9. 用户流程、异常处理与隐私

Basic：打开网页 → 设置学期与作息 → 填课程 → 核对真实日期 → 导出 → 按设备指引添加到专用日历 → 核对首末周、单双周、地点和提醒 → 自愿反馈。

Pro：完成课表 → 明确云端保存与学期有效期 → 建立编辑访问方式 → 保存 → 复制/打开订阅链接 → Apple Calendar 确认订阅 → 网站改课 → 等待客户端刷新并核对。升级时提示处理已有 Basic 导入日历，避免两套事件并存。

异常处理：
- 周次格式错误、作息未配置、无有效课次：定位到字段并保留草稿。
- 时间重叠：显示冲突，由用户确认；未确认和缺必填项才阻止输出。
- 节假日与调休：不自动推断学校安排，允许后续例外规则；未实现时给出明确限制。
- 浏览器存储不可用/刷新丢失：提示备份和恢复路径，不假装保存成功。
- 手机无法直接打开 ICS：提供已实测的替代添加路径，不宣称所有 iPhone 都能“一键全部添加”。
- Pro 网络失败、保存冲突、过期或链接失效：说明状态、重试/恢复入口，不静默丢失编辑。
- 日历没更新或提醒未响：提供检查订阅状态、网络、客户端刷新及通知设置的简短指引，不承诺立刻刷新。

Basic 课程仅在浏览器处理；若启用本地持久化，明确保存位置、共享设备风险与一键清除，默认不上传课程或图片。静态托管仍可能产生普通访问日志，应如实说明。
Pro 仅保存提供服务所需课表和凭证信息；支持导出、删除、撤销链接，公布保留期和到期处理。反馈截图先脱敏；不强制提交完整课表，不把课程名、教师、教室和令牌用于流量分析。

## 10. 优先级与阶段验收

| 阶段 | 工作与通过标准 |
|---|---|
| P0 基线核对 | 检查实际代码和旧文档，列出已实现/待验证/缺失；确认第一周定义和节次映射，避免重写已有模块 |
| P1 Basic 正确性 | 手动录入全部字段、多时段、单双周、不连续周、提醒和整学期预览可用；首末周、跨年、中文转义、唯一稳定身份等针对性测试通过，预览与导出一致 |
| P2 Basic 实机闭环 | iPhone 实际目标浏览器及可用 Mac 完成添加；核对日期/地点/提醒，验证失败回退、切后台和草稿恢复。未测平台标明，不以桌面模拟代替真机 |
| P3 33 人验证 | 招募前 33 名免费体验，记录邀请/开始/导出/实际添加人数、耗时、错误及人工支持时间；再决定收费与 Pro 投入 |
| P4 Pro 技术验证 | 一份真实订阅在 Apple Calendar 添加成功；网站新增、改时、改地点、删课后均通过客户端刷新验证；记录延迟，验证稳定 UID、缓存和到期行为 |
| P5 Pro 小规模试用 | 编辑鉴权、不可枚举只读 URL、数据隔离、备份恢复、撤销、错误恢复和成本告警可用，明确学期有效期，再试验 9.9 元价格 |

最高优先级是日期正确、真实添加成功和输入不丢失；其次为移动端操作与反馈闭环；再是 Pro。OCR 精度优化、PWA、额外学校模板等放后，不因已有功能而无限扩展。

## 11. 小红书验证与继续/停止标准

用真实问题和真实演示招募：“大学课表放进 Apple Calendar，前 33 名一起测试”。展示从输入到真实日历的完整过程，说明手动输入和核对成本；禁止未验证的“3 分钟”“自动准确识别”“永久同步”等宣传。

首轮可用约 7 天、3–5 条真实演示作为可调整实验安排。每位参与者尽量完成一次真实导入；Pro 就绪后邀请其中愿意参与者验证订阅与修改，不把 Basic 的成功人数当成订阅成功人数。

记录：设备/浏览器、课表复杂度、录入与核对耗时、实际成功率、错误类型、相较 WakeUp 的偏好、愿意付费的具体理由、实际付费转化、每人客服时间、服务成本。点赞收藏只作分发指标。

探索阈值可先设为 33 人中至少 20 人完成真实添加、至少 5 人说出具体付费理由；这些是待验证的决策参考，不能等同于成交证明。只有出现重复的真实价值反馈且维护可控，才继续 Pro 和收费。若手输/纠错更慢、支持负担持续过高或影响学习，优先简化；必要时停止新招募并保留作品展示。停止投入前仍要完成已售学期服务或给出明确补救。

## 12. 后续项目接续清单

后续在“因为受不了……”项目内继续，先完善计划，再按阶段实现：
- [ ] 对照代码补充功能现状表，保留已有 OCR 和有效 ICS 实现。
- [ ] 核实 Basic 草稿持久化、节次映射、提醒及手机添加路径。
- [ ] 修订旧 UID 方案的适用范围，真实测试重复导入，不承诺自动去重。
- [ ] 写出 P1/P2 的具体测试数据与真机验收记录。
- [ ] 确定免费内测招募与反馈表，区分“愿意付费”和实际成交。
- [ ] 达到验证条件后，比较并选择最简 Pro 托管/数据/鉴权组合。
- [ ] 明确学期有效期、到期政策、编辑恢复、隐私说明和成本上限。
- [ ] 所有新增决定记录日期、原因和证据；未做的功能保持“规划/待验证”标签。

本次仅新增本文档；不修改应用代码，不覆盖旧计划，不执行部署或收款。后续用户在该项目继续时，可从本清单接续。
