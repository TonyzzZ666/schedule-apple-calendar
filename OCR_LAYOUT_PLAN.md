# 课表 OCR 布局解析方案
## 目标与原则
在浏览器本机处理图片，结合坐标、课块、行列和文字层级，输出可核对的课程与上课安排。禁止拼接整屏 OCR 后猜字段；禁止补齐截图省略或遮挡的文字；不确定信息保留原文。本文描述目标设计，具体落地范围见末尾与 OCR_VALIDATION.md。

## 顺序
1. 保留图片原始尺寸及预处理坐标变换。OCR 文本片段保留包围盒和置信度，识别框高度仅作为字号估计，不等同真实字号。
2. 定位课表、星期表头、节次轴。支持完整周视图、部分星期、明确单日视图及未知表头。缺表头不推定今天。
3. 在列内综合颜色、矩形边缘、主流列宽与对齐检测课块。透明壁纸不等于平色；白底不能一律排除。整块跨多节仍是一条安排。
4. 将文字按坐标归属课块。星期按列映射；节次按区域重叠映射；时间只使用可读时间轴或用户确认的作息表。下一节开始时间不能冒充上一节结束时间。
5. 块内先视觉分区：分隔横线、显著空白、持续字号差异、上下位置、定位符号联合判断；文字格式只辅助。教师、地点、顶部备注分别处理。不确定文字进入未分类信息。
6. 对小字单独增强识别，保留原图和原始文字。纠错仅作有证据的候选，不静默替换专业名词。省略号不补全。
7. 每个块一个安排；同名课程可合并，但地点、时间仍按安排独立保存。未经确认不导出。

## 图片版式
- 测试1：浅底深色字、大标题、小地点、定位图标。
- 测试3：顶部标题和底部地点之间的大空白。
- 测试4：壁纸、半透明卡片、顶部标签、省略文本。
- 测试5：内部横线是地点分界；只有节次，不能猜钟点。
- 测试6：标题、教师、横线、地点；教师不能并入地点。
- 测试7：同字号多字段混排，视觉证据不足时保留未分类信息。

## 建议交换结构
每个课块保存 view_type、weekday、date、start_section、end_section、start_time、end_time、course_name、location、teacher、remarks、unclassified_text、raw_text、bbox、needs_review、review_reasons。
星期为 1–7，未知数值/日期/时间为 null，未知文本为空字符串。内部兼容已有课程/时段编辑模型，不要求立即改变 ICS 数据结构。

## 验收
六张真实截图固定为回归集。分别核对课块召回、误合并、完整课名、地点与教师分界、星期、节次、钟点。合成测试只验证局部算法，不能代替真实准确率。明确记录已实现能力和剩余失败；不得以输出课程数量代表识别成功。

## 本轮实现
在现有纯前端流程加入内部横线与大空白分界、标题与教师分层、原始块证据输出及未确定分界提示。进一步能力以实际测试记录为准；不宣称已通用支持白底/单日/复杂透明卡片。

## 高饱和度文字增强
对主底色通道差超过70且具有较暗通道的课块，追加一份颜色通道增强图：选择底色值最低的通道，提高浅色字相对底色的对比，抑制弱背景波动，再映射为白底黑字。保留原增强图；新结果 OCR 置信度高出5分且中文数量不低于原结果80%时才采用。此阈值是筛选启发式，不代表真实准确率；两份都可能错。每块保存选择模式和置信度用于复核。
适用于浓色底浅色字，不宣称能解决透明壁纸或被裁掉的文字。代价是浓色课块增加一次OCR，移动端耗时需实测。
