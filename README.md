# 本机启动（v0.2.1）
双击项目根目录“启动课表工具.cmd”，会启动本机静态网页并打开浏览器。
正确地址为 http://127.0.0.1:4173，保持启动窗口开启，关闭窗口即停止。
识图不再支持直接双击 dist/index.html；该打开方式仅可用于手动登记。
需要本机已安装 Node.js（当前电脑已安装）。无付费服务器、业务后端或 API。
全部识别资源已保存在 dist/vendor/ocr，不再依赖外部 CDN，图片在浏览器里处理。
整个 dist 必须一同保留。输入仍未持久保存；旧页面有草稿时请先保留，切换地址不会自动迁移。
