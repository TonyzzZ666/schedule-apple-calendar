# 手机场景 · Apple 日历一键导入 开发计划(v2,纯静态)

> 目标:**国内学生**用手机完成:打开页面 → 拍照识图/手动登记 → 点「导入 Apple 日历」→ 点一下「全部添加」→ 完成。
> 分发模型:小红书**个人号**引流,**不建群**;用户下单后自动获得网站链接(链接即交付物,不公开传播)。网站本身不登录、不收集信息。
> 形态约束:基础版纯静态、无框架、零后端;Pro(订阅同步)需要一个小后端,见第六节。

## 一、现状(执行前必读)

- 纯静态本地网页:页面 `dist/index.html`,逻辑 `dist/app.js`,ICS 生成 `dist/calendar.js`,模型 `dist/course-model.js`,OCR 资源全本地(`dist/vendor/ocr`,无外部 CDN)。
- 本地启动:`node serve.cjs --open`(仅 127.0.0.1,手机打不开——本期必须部署出去)。
- 测试:`node test-*.cjs`(Node 直接 require 模块)。
- 已知缺口:输入**无持久化**(刷新/手机切后台即丢);UID 含随机数(重复导入产生重复课);导出无引导。
- 现有 ICS 实现(折行 fold、转义 escapeText、UTC 时间戳、VALARM)基本正确,**只增量改,不重写**。

## 二、Apple 平台硬约束(文案必须符合事实)

1. 网页**无法**静默写入 Apple 日历(EventKit 仅原生 App)。所有 Web 方案必须经一次用户确认——这是苹果规定的,不可绕过。
2. iOS Safari 打开 ICS:日历导入预览 → 用户点「全部添加」(一步)。本计划的体验上限就是把"到达这个预览"的距离缩到最短。
3. 重复导入同一事件时,苹果按 UID 识别:UID 相同 = 更新,不同 = 重复。所以稳定 UID 是本计划的体验核心。

## 三、执行阶段(按依赖顺序)

### M1:草稿持久化(手机场景的地基,先做)

改 `dist/app.js`(+ 可加 `dist/draft.js`):

1. 每次课程/学期数据变化时自动写入 localStorage(key 如 `draft.v1`,含 schema 版本号),debounce ~300ms。
2. 页面加载时若存在草稿:恢复并显示横幅「已恢复上次的草稿(清除)」;用户确认过导出后提示「是否清除草稿」。
3. 手机上 Safari 切后台会杀页面,没有这条手机流程直接报废——**本项完成后才算手机可用**。

完成标准:手机登记一半 → 切到微信 → 切回 Safari → 数据还在;刷新桌面浏览器同理。

### M2:ICS 幂等 + 一键导入核心

改 `dist/calendar.js` + `dist/app.js`:

1. **稳定 UID**:`course-model.js` 的 `id()` 含时间戳+随机数,每次登记都变 → 重复导入产生重复课。改为 `UID = fnv1a64hex(学期周一 + 课名 + 教师 + 星期 + 开始时间 + 周次) + "@course-calendar.local"`(FNV-1a 同步实现约 10 行,写进 calendar.js,不引依赖)。保持"每个上课日一个 VEVENT"粒度。
2. 加 `X-WR-CALNAME:课表`。
3. **导入行为(关键 spike,先验证再定稿)**:目标是点按钮后 iOS Safari **直接弹日历预览**,不经过"下载 → 去文件 App 找"。
   - 方案 A(优先验证):`const url=URL.createObjectURL(new Blob([ics],{type:"text/calendar;charset=utf-8"})); location.href=url;` —— 若 iOS Safari 直接弹「全部添加」预览,采用。
   - 方案 B(回退):现有 `a[download]` 下载 + 页面上引导「点右上角下载图标 → 点文件 → 全部添加」。
   - 桌面浏览器保持下载行为不变(UA 分支)。
   - **验证清单**:iOS Safari(方案 A 是否弹预览、导航后返回页面草稿是否还在——依赖 M1)、macOS Safari/Chrome 下载正常。
4. 新增 `test-uid.cjs`:同一课程两次构造 UID 相同;不同教师/时间 UID 不同;中文课名/含逗号引号教室的转义回归;现有 `test-*.cjs` 不回归。

完成标准:手机重复导入两次 → 日历里不重复;改一门课再导入 → 该课被更新。

### M3:移动端导出 UX

改 `dist/app.js` + `dist/index.html` + `dist/style.css`:

1. 主按钮文案「导入 Apple 日历」,触屏友好(≥44px 高),页面在手机宽度下无横向滚动。
2. 导出后显示引导面板(非 alert),按 UA 分支:
   - iOS Safari:「点下方『全部添加』即可完成」+「同一课表重复导入只会更新,不会重复」。
   - macOS:「双击下载的文件 → 日历中确认添加」。
   - **内置浏览器兜底**(微信/小红书点开的 WebView,或导入未触发时):「点右上角 ··· → 在 Safari 中打开」再操作。
   - 其他:「已下载,用日历类 App 打开导入」。
3. 文件名:`课表-2026秋.ics`(由学期周一月份派生:2–7 月=春,其余=秋,年份取学期所在年)。
4. 手机 OCR 性能验证:tesseract chi_sim 语言包约 15MB、手机上较慢——实测一次完整识图;若 >15 秒,页面加「首次识别需下载识别资源,约 1 分钟」提示,并保证手动登记路径顺畅(登记入口置顶)。

完成标准:三种 UA 文案正确;iPhone 上按钮好点、无横向滚动;手动登记全流程可用。

### M4:部署到国内静态托管(手机能打开的前提)

方案:**腾讯云 EdgeOne Pages**(国内加速、有免费额度)或 **COS 静态网站**(默认域名国内可达、免备案,绑自定义域名则需备案)。部署物就是 `dist/` 整个目录,OCR 资源全本地、零 CDN,天然适合国内。

执行内容:

1. README 增加「手机部署」章节:两种托管方式的**控制台点击步骤**(截图级描述)+ 常见问题(备案说明、缓存刷新)。
2. `serve.cjs` 保持本地模式不变;若用户想局域网调试手机,可加 `--lan` 参数(监听 0.0.0.0 + 放开 Host 白名单 + 打印局域网地址,标注防火墙提示)。仅开发期用。
3. 交付模型:链接**不公开传播**,由下单流程自动发给用户(私信/订单自动发货)。建议注册短而好记的域名;若走「兑换码」模式(支付渠道发货卡密、用户到网站兑换),兑换验证并入第六节 Pro 后端,基础版不设门禁。注意:从小红书/微信点开的链接落在内置浏览器,导入体验会打折,兜底文案见 M3。

完成标准:手机浏览器访问部署地址,完成 拍照识图 → 登记 → 导入 全流程;国内网络下页面加载时间可接受(首次 OCR 资源下载除外,见 M3-4 提示)。

### M5(可选,后续):PWA 化

`manifest.json` + 图标,「添加到主屏幕」后无地址栏、像 App。纯静态友好,约 1 小时。不在本期验收内。

## 四、验收总清单(手机为主线)

- [ ] 手机登记一半切后台再回来,草稿还在(M1)
- [ ] 手机重复导入两次 → 日历无重复;改课再导入 → 该课被更新(M2)
- [ ] iOS Safari 点导入 → 到「全部添加」预览的点击次数 ≤ 2(方案 A)或 ≤ 3(方案 B),有明确引导文案(M2/M3)
- [ ] 中文课名、含 `,;\"\n` 的教室/备注、单双周、VALARM 提醒 → 日历显示正确
- [ ] 部署地址在国内网络下可访问,手动登记+识图全流程可用(M4)
- [ ] 现有 `test-*.cjs` 全部通过;桌面端导出、本地识图流程无回归

## 五、红线(不要做)

- 不引入构建链、框架、npm 运行时依赖(保持原生 JS + 静态文件)。
- 不尝试绕过 iOS 的「全部添加」确认(苹果平台不允许,不存在合法捷径)。
- 基础版不上传课程数据/图片到任何服务器(静态托管只存代码;数据永远只在用户浏览器)。Pro 后端仅存 ICS 文本,无身份信息、学期末自动删除。
- 不订阅、不登录、不收集任何用户信息。
- 不改动 OCR 识别流程与课程模型行为,只做增量。

## 六、Pro 档:订阅 URL 自动同步(第二阶段,配合「下单自动给网站」)

交付物 = 订阅链接本身(**链接即凭证**,无需账号体系)。用户下单 → 自动发货链接 → 手机点开 → 订阅一次(一次性)→ 之后重新发布课表,苹果日历自动更新。

- **P1 后端**(腾讯云函数/EdgeOne Functions,~150 行):`POST /ics` 生成随机 128-bit id;`PUT /ics/:id`(token 校验)覆盖更新;`GET /ics/:id` 返回 `text/calendar`。学期结束自动清理。
- **P2 前端 Pro 入口**:生成/更新订阅链接、复制按钮 + 引导「点右上角在 Safari 中打开」(微信/小红书内置浏览器打不开 webcal)、同步延迟文案(「通常几小时内更新;急用请在日历里关闭再打开该订阅立即刷新」)。
- **P3 发货对接**:支付渠道自动发货卡密/链接(闲鱼卡密、淘宝虚拟商品等支持自动发货;小红书个人号能否卖虚拟商品需实测)。若用兑换码,验证逻辑并入 P1。
- 隐私:无登录、URL 不可猜、学期末自动删除、页面明示「知道链接即可访问,请勿公开」。

验收要点:iPhone 订阅一次 → 改一门课 → 重新发布 → 强制刷新订阅 → 该课被更新而非新增;发货→兑换→订阅端到端可用。
